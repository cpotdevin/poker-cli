# Agents

## Git

- Commit messages must be a single line, under 50 characters
- Never add Co-Authored-By or other trailers

## Releasing

- Push a `v*` tag to trigger the release workflow (e.g. `git tag v0.0.3 && git push origin v0.0.3`)
